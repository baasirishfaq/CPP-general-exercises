#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
    int a, b, sum = 0, diff;
    cout << "enter 2 numbers :";
    cin >> a >> b;
    sum = a + b;
    diff = a - b;
    cout << "Sum is " << sum << " Diff is " << diff;
}
