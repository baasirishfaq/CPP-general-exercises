#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
    int prod, arr[5];
    cout << "enter 5 nums into array :";

    for (int i = 0; i < 5; i++)
    {
        cin >> arr[i];
    }
    // cout << "TEST PRINT " << endl;
    // for (int i = 0; i < 5; i++)
    // {
    //     cout << arr[i];
    // }

    for (int i = 0; i < 5; i++)
    {
        cin >> arr[i];
    }
}